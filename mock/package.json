{
  "name": "rt-recommendation-agent-mock",
  "version": "1.0.0",
  "description": "Mock Azure Function for RT Recommendation Agent — Vetsource Hackathon 2026",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "npm run build && func start",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3"
  }
}
